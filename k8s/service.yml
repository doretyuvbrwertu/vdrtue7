apiVersion: v1
kind: Service
metadata:
  name: mailwd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "mailwd"
    port: 8080
  selector:
    app: mailwd
