apiVersion: v1
kind: Service
metadata:
  name: dveihui
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dveihui"
    port: 8080
  selector:
    app: dveihui
