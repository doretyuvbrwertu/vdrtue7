apiVersion: v1
kind: Service
metadata:
  name: piuiudur
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "piuiudur"
    port: 8080
  selector:
    app: piuiudur
