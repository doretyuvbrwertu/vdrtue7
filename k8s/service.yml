apiVersion: v1
kind: Service
metadata:
  name: mailwee
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "mailwee"
    port: 8080
  selector:
    app: mailwee
