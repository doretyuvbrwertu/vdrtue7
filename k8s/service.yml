apiVersion: v1
kind: Service
metadata:
  name: ruiuder
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ruiuder"
    port: 8080
  selector:
    app: ruiuder
