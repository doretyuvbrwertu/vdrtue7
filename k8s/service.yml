apiVersion: v1
kind: Service
metadata:
  name: hinweid
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hinweid"
    port: 8080
  selector:
    app: hinweid
