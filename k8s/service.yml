apiVersion: v1
kind: Service
metadata:
  name: zeiuder
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "zeiuder"
    port: 8080
  selector:
    app: zeiuder
