apiVersion: v1
kind: Service
metadata:
  name: weid
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "weid"
    port: 8080
  selector:
    app: weid
