apiVersion: v1
kind: Service
metadata:
  name: postweid
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "postweid"
    port: 8080
  selector:
    app: postweid
