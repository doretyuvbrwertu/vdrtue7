apiVersion: v1
kind: Service
metadata:
  name: weidmail
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "weidmail"
    port: 8080
  selector:
    app: weidmail
