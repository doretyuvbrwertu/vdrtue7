apiVersion: v1
kind: Service
metadata:
  name: yeiveiutou
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yeiveiutou"
    port: 8080
  selector:
    app: yeiveiutou
