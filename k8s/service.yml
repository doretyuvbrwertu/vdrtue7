apiVersion: v1
kind: Service
metadata:
  name: asdewue
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "asdewue"
    port: 8080
  selector:
    app: asdewue
